
const getCreationBrand = state => state.brands.create;
const getBrands = state => state.brands.list;

const CollectionsSelectors = ({
  getCreationBrand,
  getBrands,
});

export default CollectionsSelectors;
